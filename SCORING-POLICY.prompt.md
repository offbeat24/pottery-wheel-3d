# 점수·판매가 정책 프롬프트

이 파일은 **그대로 복사해 프롬프트에 붙이는 용도**다. 구현·검증이 끝난 정책의 상수와 공식,
새 도구를 붙일 때 따라 할 절차, 반드시 지켜야 할 불변식, 검증 방법을 한곳에 모았다.

- 제품 방향의 원천은 `DESIGN.md`다. 이 파일은 그중 점수·값 부분을 프롬프트에 바로 넣을 수 있게 뽑아 정리한 것이다.
- 정책을 바꾸면 `DESIGN.md`, 이 파일, `tests/scoring.test.ts`를 같은 작업에서 함께 고친다.
- 구현 위치: `src/game/scoring.ts`(규칙) · `src/game/orders.ts`(주문별 요구) · `src/game/gallery.ts`(저장) · `src/main.ts`(측정·표시)

---

## 1. 판매가는 두 층으로 계산된다

**곡선 안에 들어가는 것과 곡선 밖에서 곱하는 것을 절대 섞지 마라.** 같은 0.65배라도 효과가 완전히 다르다.

```
판매가 = round( (1000 + 29000 × (총점 × 굽기 / 100)^2.4) × 작업속도 × 공방배수 / 100 ) × 100
                              └── 곡선 안 ──┘              └──── 곡선 밖 ────┘
```

| 값 | 어디에 곱하는가 | 범위 | 효과 |
|---|---|---|---|
| **총점** `scoreClay(단면, 주문, 트임, 새김기록)` | 곡선 안 | 0~100 | 지수 2.4로 증폭 |
| **굽기** `firingQuality(꺼낸초)` | **곡선 안 (총점에 곱한다)** | 0.5~1 | 지수로 증폭 — 아래 주의 |
| **작업 속도** `paceMultiplier(걸린초, 만진초)` | 곡선 밖 | 0.7~1.35 | 배수 그대로 |
| **공방 배수** `priceMultiplier(owned)` | 곡선 밖 | 1 / 1.3 / 1.6 / 2 | 배수 그대로 |

- **점수곡선**: 위로 가파른 곡선. 30점 2,300원 · 60점 9,700원 · 87점 21,800원 · 100점 30,000원.
- **굽기 완성도** `firingQuality(초)`: 35초 ±3초 안이면 1.0. 벗어나면 `1 - 벗어난초/(이르면 22, 늦으면 34)`, 하한 0.5. 설익은 쪽이 더 아프다.
  ⚠️ **굽기는 값이 아니라 총점에 곱한다.** 그래서 곡선을 한 번 더 통과하며 증폭된다.
  87점 작품: 굽기 1.0 → 21,800원 · 굽기 0.65 → **8,500원(39%)** · 굽기 0.5 → **5,000원(23%)**.
  "0.65배 깎인다"고 읽으면 안 된다. 가마 타이밍이 이 게임에서 가장 비싼 실수다.
- **작업 속도·공방 배수**는 곡선 밖이라 그대로 곱해진다. 87점: 속도 ×1.35 → 29,400원 · ×0.7 → 15,200원.

**실효 범위**: 상한 100점 × 굽기 1.0 × 속도 1.35 × 공방 2 = **81,000원**, 하한 0점 × 속도 0.7 = **700원**.
실패작도 값이 0은 아니다. (`tests/scoring.test.ts`의 「판매가 구성」이 이 숫자를 고정한다.)

### 총점 배점

`scoreClay(단면, 주문, 트임, 새김기록)`이 낸다.

```
기법 요구가 없는 주문:  실루엣 0.65 · 높이 0.25 · 매끄러움 0.10
기법 요구가 있는 주문:  실루엣 0.50 · 높이 0.20 · 매끄러움 0.10 · 기법 0.20
```

- 실루엣 = `(1 - 평균오차/0.42) × 100`, 높이 = `(1 - |높이차|/0.72) × 100`
- 매끄러움은 표면 트임으로 최대 80%까지 깎인다: `× (1 - 트임 × 0.8)` — 마른 흙을 밀어 튼 표면이 값으로 이어지는 경로다.

### 값이 표시되는 두 시점이 다르다

| 화면 | 계산 | 굽기 반영 |
|---|---|---|
| 완성 화면 `#result-price` | `sellPrice(점수, 공방 × 속도)` | **없음** — 아직 굽지 않았으므로 굽기 전 추정값이다 |
| 선반 판매 토스트 | `sellPrice({...점수, 총점: round(총점 × 굽기)}, 공방 × 속도)` | 있음 |

완성 화면의 값보다 실제 판매가가 낮게 나오는 것은 정상이다(굽기 ≤ 1). 이 차이를 없애려면 굽기를 예측해 보여주는 것이 아니라, 완성 화면 문구가 추정값임을 드러내야 한다.

### 값을 잃는 세 경로 (모두 의도된 규칙이다)

1. **가마를 늦거나 이르게 꺼낸다** — 굽기가 총점에 곱해져 곡선 안에서 증폭된다. 가장 비싼 실수.
2. **굽는 중에 다음 작품을 완성한다** — 가마는 한 점만 받으므로 앞 작품이 **그 시점의 굽기 완성도로 강제 배출**된다(`loadKiln`이 먼저 `unloadKiln`을 부른다). 25초에 다음 작품을 완성하면 앞 작품은 굽기 0.5에 가까워진다.
3. **같은 주문을 다시 완성한다** — 선반 슬롯은 주문 하나당 하나여서 그 자리의 앞 작품이 교체되고, 팔지 않은 앞 작품의 값은 사라진다.

### 누적 수익과 지출

- 판매가는 그대로 `공방 수익`에 더해지고 `localStorage`에 정수로 저장된다(음수·숫자 아님은 0으로 읽는다).
- 지출은 상점뿐이다. 도구 조각칼 25,000원 · 공방 60,000 / 140,000 / 260,000원(×1.3 / ×1.6 / ×2) · 선반 30,000 / 90,000 / 160,000원(외형만).
- 같은 갈래를 여러 개 가지면 `SHOP_ITEMS` **배열의 뒤쪽**이 적용된다. 등급은 배열 순서로만 표현하고 등급 필드를 두지 않는다.
- 한 점을 아무리 잘 만들어도(상한 81,000원) 상위 공방(260,000원)을 살 수 없게 둔다. 상점은 여러 점의 누적으로만 채워진다.

---

## 2. 작업 속도 정책 (구현·검증 완료)

```ts
PACE_SECONDS = 60      // 기준 시간
MAX_PACE = 1.35        // 상한
MIN_PACE = 0.7         // 하한
PACE_WORK_SECONDS = 8  // 보너스를 다 받는 데 필요한 '흙 만진 시간'

배수 = clamp(1 + (60 - 걸린초)/60 × 0.5, 0.7, 1.35)
배수 > 1 이면: 1 + (배수 - 1) × min(1, 만진초/8)
```

- 같은 점수라면 빨리 빚어낸 손이 더 받는다. 점수 곡선에 **곱해지므로** 빨리 만든 실패작은 여전히 싸다.
- **보너스는 흙을 만진 시간에 비례한다.** 초기 점토가 첫 주문과 이미 닮아 있어서, 손대지 않고 바로 완성해 최고 배수를 챙기는 길이 열려 있었다. 감점 쪽은 만진 시간과 무관하게 그대로 적용한다.
- 시계는 주문 도착부터 돌고, 결과 화면과 상점을 보는 동안 멈춘다. 다시 빚기·다시 시작·다음 주문에서 0으로.
- 경과 시간은 HUD `작업 시간` 행에 상시 표시, 완성 화면 머리줄에 `1분 30초 · 작업 속도 ×0.74`.
- 배수는 저장본에 `pace` 한 숫자로 남긴다(판매는 나중에 선반에서 일어나 그때 시간을 다시 셀 수 없다).

**검증된 값** — 같은 73점 아침 찻잔: 15초 ×1.35 → 19,700원 / 1분 30초 ×0.74 → 10,900원.

---

## 3. 도구와 기법 정책 (구현·검증 완료)

### 원칙

- 도구는 능력치가 아니라 **라운드를 여는 열쇠**다. 조각칼 없으면 9라운드, 있으면 11라운드.
- 도구가 열어준 라운드는 형태만으로 판정하지 않는다. 그 도구로 **무엇을, 어디에, 어떤 순서로** 했는지를 「기법」으로 따로 본다.
- 요구는 주문서에 그대로 적는다. 숨은 요구로 점수가 깎이면 규칙이 아니라 사고로 읽힌다.
- 도구를 사지 않은 플레이어의 점수는 흔들리지 않는다(기법 요구가 없는 주문은 예전 배점 그대로).

### 주문에 적는 요구

```ts
requires: CARVING_KNIFE,
technique: { tool: CARVING_KNIFE, grooves: [0.6, 0.68, 0.76], tolerance: 0.05, ordered: true },
```

| 필드 | 뜻 |
|---|---|
| `tool` | 이 기법에 필요한 도구. `requires`와 같아야 한다. |
| `grooves` | 새겨야 하는 높이(0~1). **아래에서 위 순서로** 적는다. |
| `tolerance` | 홈 하나가 인정되는 높이 오차. |
| `ordered` | true면 적은 순서대로 새겨야 한다. |

### 기법 점수가 보는 네 가지

```ts
TECHNIQUE_WEIGHT = 0.2        // 총점 가중치
CORE_RATIO = 0.4              // 오차 한계 중 만점으로 인정하는 심
MIN_GROOVE_DEPTH = 0.03       // 홈으로 인정되는 최소 깊이
ORDER_PENALTY = 0.35          // 순서를 다 어겼을 때 감점
EXTRA_GROOVE_PENALTY = 0.12   // 군더더기 한 줄마다 감점(하한 40%)
```

1. **자리** — 요구 높이에서 벗어난 만큼 깎는다. 오차 한계의 40%(`core`) 안쪽은 맞춘 것으로 보고, 거기서 한계까지 직선으로 내려가며, 한계를 넘으면 그 줄은 0점이다.
   `정확도 = 오차 ≤ core ? 1 : 1 - (오차 - core)/(한계 - core)`
   **심(core)이 반드시 있어야 한다.** 커서로 조준할 수 있는 정밀도가 높이 2%p 남짓이라, 심 없이 직선 감점만 두면 정확히 겨눠도 만점이 구조적으로 불가능하다(실측 68점).
2. **깊이** — 저장된 **실제 단면**에서 홈이 남아 있는지 잰다. `깊이점수 = min(1, 실측깊이/0.03)`.
   실측은 위아래 어깨 평균에서 가운데를 뺀 값이고, 어깨 간격은 `min(0.07, 한계 × 0.8)`이다.
   **어깨가 옆 홈까지 닿으면 깊이가 얕게 나온다.** 홈이 촘촘한 주문에서는 반드시 좁혀 재야 한다.
   이 항 덕분에 새겼다는 기록만 남고 물로 다듬어 뭉개졌으면 점수도 함께 사라진다 → 새김은 물 다듬기 **뒤에**.
3. **순서** — `ordered` 주문은 뒤집힌 짝의 비율만큼 최대 35%를 깎는다.
4. **군더더기** — 요구보다 많이 새긴 줄마다 12%를 깎고 40%에서 멈춘다. 같은 자리 덧파기(3%p 안쪽)는 새 줄로 세지 않는다.

`한 줄 = 자리 정확도 × 깊이`를 모두 더해 요구 줄 수로 나누고, 순서·군더더기 배수를 곱한다.

### 라운드가 올라가면 요구가 커진다

| 라운드 | 주문 | 줄 수 | 오차 | 순서 |
|---|---|---|---|---|
| 9 | 새김무늬 잔 | 1 | 10%p | 없음 |
| 10 | 세 줄 새김 항아리 | 3 | 5%p | 있음 |

새 도구를 넣을 때도 같은 계단을 따른다: **첫 주문은 하나 · 넉넉한 오차 · 순서 없음**, 다음 주문에서 개수를 늘리고 오차를 좁히고 순서를 요구한다.

**검증된 값** — 세 줄 새김 항아리(굽기 100%): 순서대로 기법 86 / 총점 83 / 19,500원 · 뒤집으면 기법 58 / 총점 77 · 안 새기면 기법 0 / 총점 65 / 11,300원.

---

## 4. 새 도구를 추가할 때 (아이템 목록이 오면 이 절차대로)

1. `src/game/shop.ts`의 `SHOP_ITEMS`에 도구를 넣고 상수를 export한다.
2. `src/game/orders.ts`에 그 도구 전용 주문을 **두 개 이상** 넣고, 각 주문에 `requires`와 `technique`를 나란히 적는다. 목표 단면에는 그 기법으로 만들어지는 형태가 실제로 들어 있어야 한다(홈을 요구하면 단면에도 홈이 있어야 한다).
3. 난이도는 3절의 계단을 따른다.
4. `src/main.ts`에서 그 도구의 조작을 기록에 남긴다(조각칼은 `carvings.push(높이)`).
5. 테스트를 추가한다: 요구를 채우면 99점 이상, 빠뜨리면 85점 미만, 순서를 어기면 내려간다, 군더더기는 깎인다.
6. `DESIGN.md`와 이 파일을 같은 작업에서 갱신한다.

`grooves`는 「홈 높이」에 특화된 이름이다. 물레칼·굽칼처럼 **높이 지점을 지정하는** 도구는 그대로 쓰고,
성질이 다른 도구(표면 전체에 바르기, 손잡이 붙이기 등)가 오면 그때 `technique`에 종류 필드를 열어 갈래를 나눈다.
지금 쓰지 않을 갈래를 미리 만들지 않는다.

---

## 5. 반드시 지켜야 할 불변식

- **점수는 저장하지 않는다.** 저장본은 `{ height, outerRadii, damage, firing, pace, carvings }`이고, 판매 시점에 이 값으로 다시 계산한다. 채점 기준이 바뀌면 예전 작품의 값도 함께 바뀐다.
- 다시 계산할 때 단면은 `buildSafeProfile(height, outerRadii)`을 한 번 통과한다. 벽 두께·반지름 한계를 다시 적용하므로 완성 화면의 점수와 몇 점 차이가 날 수 있다. 정상이며, 둘을 강제로 일치시키려 점수를 저장하지 마라.
- 저장본에 필드를 더하면 `parseGallery`에 기본값과 범위 자르기를 함께 넣는다(localStorage는 사용자가 고칠 수 있다). 배열은 길이 상한도 둔다.
- 기법 요구가 **없는** 주문의 총점 배점은 건드리지 않는다.
- 요구는 화면에 보여준다. 주문 카드의 요구 문구와 조작 직후 토스트가 없으면 정책을 넣은 것이 아니다.
- 감점만으로 값을 0으로 만들지 않는다. 실패도 다음 작업으로 넘어가는 리듬이다.
- 카운트다운, 시간 초과 실패, 도구 내구도, 순위표는 범위 밖이다. 속도와 기법은 압박 장치가 아니라 숙련의 보상이다.

---

## 6. 검증 절차 (기계 검증만으로는 부족하다)

`npm run verify`(bass doctor · typecheck · unit · e2e 1440×900 · build)는 기본이고, **아래 실전 확인을 반드시 거친다.**
심(core) 문제는 유닛 테스트를 모두 통과한 상태에서 실전 확인으로만 드러났다.

1. 도구를 물린 저장 상태로 실제 브라우저 1440×900에서 해당 라운드까지 간다.
2. HUD가 알려주는 값으로 조준한다. 화면 좌표를 짐작하지 말고 `#contact-state`의 `높이 NN%`를 읽어 목표에 가장 가까운 위치를 찾는다.
3. 같은 주문을 세 번 만든다: **요구대로 / 순서를 뒤집어 / 요구를 빠뜨려**. 완성 화면의 기법·총점·값이 세 갈래로 벌어지는지 본다.
4. **요구대로 정확히 했을 때 기법이 충분히 높은지 확인한다.** 정확히 겨눴는데 70점대라면 감점 곡선이 가파른 것이다.
5. 저장→판매까지 확인한다: 완성 → 가마에서 알맞은 때에 꺼내기 → **새로고침** → 선반 작품 두 번 클릭. 새로고침 후 판매가가 요구를 채운 쪽과 빠뜨린 쪽으로 갈리면 기록이 살아남은 것이다.
   이때 굽기를 100%로 맞춰야(알맞은 때에 꺼내야) 두 값을 공정하게 비교할 수 있다. 굽기가 다르면 곡선 안에서 증폭되어 비교가 무의미해진다.
6. 알아둘 것: 조각칼 주문 작품은 **위쪽 선반**에 놓여 기본 시점에서 화면 밖이다. 팔려면 시선을 올려야 한다.
7. 브라우저 자동화로 확인할 때는 headed로 돌린다. 탭이 가려지면 rAF가 조절되어 가마 시계가 멈춘 것처럼 보인다.
   저장 상태를 심는 초기 스크립트는 새로고침에도 다시 실행되므로, 첫 진입에만 심도록 표시를 남긴다.
