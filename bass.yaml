# BASS 프로젝트 설정. BASS 코드를 복사하지 않고 버전을 의존한다.
bass:
  version: 0.2.1
  profiles:
    - common
    - web

project:
  name: pottery-wheel-3d
  description: 브라우저에서 점토를 직접 빚는 감각을 탐구하는 3D 도예 게임

# capability alias 만 사용한다. 모델명을 직접 쓰지 마라.
# 특정 모델 고정이 꼭 필요하면 "pin:provider/model" 표기를 쓴다.
models:
  worker: auto

workflow:
  max_active_tasks: 2
  reviewer_required: true

evaluators:
  level1:
    - name: typecheck
      command: npm run typecheck
  level2:
    - name: unit
      command: npm test
    - name: browser
      command: npm run test:e2e
      timeout_ms: 180000
    - name: build
      command: npm run build
  level3: []
